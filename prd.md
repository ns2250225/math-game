已将**分享结果功能**作为核心传播模块加入，包括成绩分享卡片、图片生成、Web Share API、复制文案、挑战链接、隐私控制和纯前端实现方案。

# 算术冲刺游戏网站 PRD

## 1. 文档信息

| 项目      | 内容                       |
| ------- | ------------------------ |
| 产品名称    | 算术冲刺 Math Rush           |
| 产品类型    | 纯前端算术反应小游戏               |
| 文档版本    | V1.0                     |
| 运行平台    | PC Web、移动端 Web、PWA       |
| 是否需要服务器 | 否                        |
| 数据存储    | LocalStorage / IndexedDB |
| 核心玩法    | 答题延长时间，答题越多，时间流逝越快       |
| 核心特色    | 动态难度、连击、随机事件、成绩分享、好友挑战   |

---

# 2. 产品概述

## 2.1 产品定位

“算术冲刺”是一款以心算速度和反应能力为核心的纯前端休闲小游戏。

游戏开始后，系统持续随机生成加、减、乘、除算术题。玩家需要在剩余时间归零前输入正确答案。每答对一道题可以获得时间奖励，但随着答对题数不断增加，倒计时流逝速度也会逐渐加快。

玩家需要在“答题增加时间”和“时间加速消耗”之间维持平衡，挑战更高分数。

游戏结束后，系统会生成一张个性化成绩分享卡片，玩家可以保存图片、复制分享文案或调用系统分享功能，将成绩分享给好友。

## 2.2 产品口号

> 算得越快，时间跑得越快。

备选口号：

* 你能追上时间吗？
* 答对续命，越强越快。
* 30 秒开始，看看你能撑多久。
* 时间不会等你，答案也不能犹豫。

## 2.3 产品目标

1. 提供简单易懂、无需学习成本的算术小游戏。
2. 通过动态倒计时机制增加紧张感和重复游玩动力。
3. 通过连击、技能题和特殊事件增强游戏性。
4. 通过成绩卡片与好友挑战实现自然传播。
5. 保持纯前端架构，无需账号、服务器和数据库。
6. 支持离线访问和安装为 PWA 应用。

---

# 3. 目标用户

## 3.1 核心用户

* 希望练习心算能力的学生。
* 喜欢轻量反应类小游戏的用户。
* 希望利用碎片时间进行脑力训练的上班族。
* 喜欢挑战高分、刷新纪录的玩家。
* 希望与朋友比较反应速度的社交用户。

## 3.2 使用场景

* 上下班途中玩一局。
* 课间练习心算。
* 朋友之间互相挑战。
* 工作间隙进行短暂脑力训练。
* 将成绩卡分享到社交平台。
* 通过好友分享的挑战链接进入游戏。

---

# 4. 产品核心循环

```text
进入首页
  ↓
选择游戏模式
  ↓
开始游戏
  ↓
系统生成算术题
  ↓
玩家输入答案
  ↓
判断答案
  ├─ 正确：增加时间、增加分数、累计连击
  └─ 错误：连击中断、可能扣除时间
  ↓
生成下一道题
  ↓
时间流逝速度不断提高
  ↓
剩余时间归零
  ↓
展示本局统计
  ↓
生成成绩分享卡片
  ↓
再来一局 / 分享成绩 / 挑战好友
```

---

# 5. 核心游戏规则

## 5.1 初始时间

默认初始时间为：

```text
30 秒
```

可在设置中提供不同档位：

| 难度 | 初始时间 |
| -- | ---: |
| 休闲 | 45 秒 |
| 标准 | 30 秒 |
| 困难 | 20 秒 |
| 极限 | 10 秒 |

默认游戏模式使用标准难度。

## 5.2 时间流逝

游戏开始后，剩余时间持续减少。

基础状态下：

```text
现实经过 1 秒，游戏时间减少 1 秒。
```

随着玩家答对数量增加，时间流逝倍率逐步提高。

|  累计答对数量 | 时间流逝倍率 |
| ------: | -----: |
|     0～9 |  1.0 倍 |
|   10～19 |  1.2 倍 |
|   20～34 |  1.5 倍 |
|   35～49 |  1.8 倍 |
|   50～69 |  2.2 倍 |
|   70～99 |  2.8 倍 |
| 100 题以上 |  3.5 倍 |

示例：

当时间流逝倍率为 2 倍时，现实经过 1 秒，游戏剩余时间减少 2 秒。

## 5.3 答对奖励

玩家答对题目后获得时间奖励。

|    答对数量 |     基础奖励 |
| ------: | -------: |
|    0～19 |   增加 2 秒 |
|   20～39 | 增加 1.8 秒 |
|   40～69 | 增加 1.5 秒 |
|   70～99 | 增加 1.2 秒 |
| 100 题以上 |   增加 1 秒 |

随着游戏进行，单题时间奖励逐渐减少，避免玩家通过简单题无限维持游戏。

## 5.4 答错处理

默认规则：

* 不增加时间。
* 当前连击数清零。
* 输入框快速震动。
* 显示正确答案 0.5 秒。
* 自动进入下一题。

可选困难规则：

* 答错额外扣除 2 秒。
* 连续答错两题，额外扣除 3 秒。
* 极限模式下答错立即结束游戏。

## 5.5 游戏结束

满足以下条件时游戏结束：

```text
剩余时间 ≤ 0
```

游戏结束后：

1. 停止倒计时。
2. 禁止继续输入。
3. 播放结束动画。
4. 计算本局数据。
5. 判断是否刷新纪录。
6. 展示结算页面。
7. 自动生成成绩分享卡片。

---

# 6. 题目生成系统

## 6.1 支持题型

游戏支持以下基础运算：

* 加法。
* 减法。
* 乘法。
* 除法。
* 混合运算，可作为后期扩展。

## 6.2 加法题

示例：

```text
8 + 7
24 + 38
157 + 286
```

生成规则：

* 操作数均为整数。
* 结果不超过当前难度允许范围。
* 避免连续生成完全相同的题目。

## 6.3 减法题

示例：

```text
15 - 8
83 - 27
412 - 185
```

默认规则：

* 保证结果大于或等于 0。
* 高难度设置可允许负数结果。

## 6.4 乘法题

示例：

```text
7 × 8
13 × 6
24 × 17
```

低难度优先生成九九乘法表范围内的题目。

随着玩家答题数量增加，逐步加入：

* 两位数乘一位数。
* 两位数乘两位数。
* 包含整十数的快速计算题。
* 接近整百数的技巧题。

## 6.5 除法题

示例：

```text
56 ÷ 7
144 ÷ 12
360 ÷ 15
```

生成方式：

1. 先随机生成除数和整数答案。
2. 将两者相乘得到被除数。
3. 保证所有除法题均可整除。
4. 除数不得为 0。
5. 默认不生成小数答案。

## 6.6 动态题型权重

游戏初期优先出现较简单的加减法，随着分数提升，乘除法占比逐渐提高。

| 阶段      |  加法 |  减法 |  乘法 |  除法 |
| ------- | --: | --: | --: | --: |
| 0～10 题  | 35% | 35% | 20% | 10% |
| 11～30 题 | 25% | 25% | 30% | 20% |
| 31～60 题 | 20% | 20% | 30% | 30% |
| 60 题以上  | 15% | 15% | 35% | 35% |

如果玩家在设置中关闭某种题型，则系统在剩余题型中重新分配权重。

## 6.7 动态难度

难度不仅根据答对数量调整，还需要参考玩家最近的答题速度。

参考指标：

* 最近 10 题平均答题时间。
* 最近 10 题正确率。
* 当前最长连击。
* 当前时间流逝倍率。
* 当前剩余时间。

调整规则示例：

* 最近平均答题时间低于 1 秒：提高数字范围。
* 最近平均答题时间低于 0.7 秒：提高乘除法比例。
* 连续答错两题：暂时降低下一题难度。
* 剩余时间低于 5 秒：减少超长数字题的出现概率。
* 连续答对 20 题：进入高压题库。

---

# 7. 游戏模式

## 7.1 经典生存模式

默认核心模式。

规则：

* 初始时间为 30 秒。
* 答对增加时间。
* 时间不断减少。
* 答题越多，时间流逝越快。
* 时间归零后结束。

适合追求最高分和最长生存时间。

## 7.2 百题竞速模式

规则：

* 固定完成 100 道题。
* 时间从 0 开始正向计时。
* 答错需要重新回答当前题，或增加时间惩罚。
* 最终以完成时间作为成绩。

分享重点：

```text
我用 2 分 38 秒完成了 100 道算术题。
```

## 7.3 极限一命模式

规则：

* 初始时间较短。
* 答错一道题立即结束。
* 时间流逝速度提升更快。
* 分数系数更高。

## 7.4 闪现记忆模式

规则：

* 题目只显示 1 秒。
* 随后题目隐藏。
* 玩家需要根据记忆输入答案。
* 后期显示时间逐渐缩短。

## 7.5 每日挑战

使用当前日期作为随机种子，每天生成固定的题目序列。

特点：

* 同一天所有玩家使用相同题目。
* 不需要服务器也能保持题目一致。
* 成绩保存在本地。
* 分享卡片显示挑战日期。
* 分享链接可以携带每日挑战模式参数。

限制：

由于没有服务器，无法提供可信的全球排行榜，但可以展示本地历史排名和好友自行比较成绩。

---

# 8. 连击系统

## 8.1 连击规则

每连续答对一道题：

```text
Combo +1
```

答错后：

```text
Combo = 0
```

## 8.2 连击阶段

| 连击数量 | 称号    | 表现        |
| ---: | ----- | --------- |
|    5 | 热身完成  | 数字轻微发光    |
|   10 | 火力全开  | 出现火焰粒子    |
|   20 | 心算高手  | 背景节奏增强    |
|   30 | 算术狂人  | 题目切换带冲击效果 |
|   50 | 人形计算器 | 全屏高光提示    |
|  100 | 数学之神  | 解锁特殊成绩卡   |

## 8.3 连击分数倍率

|   连击数 |  得分倍率 |
| ----: | ----: |
|   0～4 |   1 倍 |
|   5～9 | 1.1 倍 |
| 10～19 | 1.2 倍 |
| 20～39 | 1.5 倍 |
| 40～69 |   2 倍 |
| 70 以上 |   3 倍 |

---

# 9. 特殊题目与随机事件

## 9.1 幸运题

随机出现带有星星标识的幸运题。

答对奖励：

* 增加 5～8 秒。
* 获得额外分数。
* 播放金色粒子动画。

出现概率建议为 3%。

## 9.2 炸弹题

题目出现时附带独立的短倒计时。

规则：

* 必须在 2～3 秒内答对。
* 超时扣除 3 秒。
* 答对奖励额外分数。

## 9.3 冰冻题

答对后触发时间冻结：

```text
倒计时暂停 3 秒
```

冻结期间玩家仍可以继续答题。

## 9.4 双倍题

答对后：

* 本题分数翻倍。
* 时间奖励翻倍。
* 连击增加 2。

## 9.5 Boss 题

每答对 20 题触发一次 Boss 题。

特点：

* 数字更大。
* 计算步骤更复杂。
* 具有独立限时。
* 答对获得大量时间和分数。
* 答错不立即结束，但会扣除时间。

示例：

```text
247 + 583
36 × 14
864 ÷ 18
```

## 9.6 Fever 模式

连续答对指定数量后进入 Fever 状态。

建议条件：

```text
连续答对 15 题
```

持续时间：

```text
5 秒
```

Fever 效果：

* 主倒计时暂停或减速。
* 得分翻倍。
* 背景变为高亮状态。
* 正确反馈动画增强。
* 音乐进入高潮段落。

---

# 10. 页面结构

```text
首页
├── 开始游戏
├── 模式选择
├── 最高纪录
├── 每日挑战
├── 历史记录
├── 成就系统
├── 游戏设置
└── 玩法说明

游戏页面
├── 剩余时间
├── 时间进度条
├── 时间流逝倍率
├── 当前分数
├── 答对数量
├── 当前连击
├── 算术题目
├── 答案输入框
├── 状态反馈
└── 暂停按钮

结算页面
├── 最终成绩
├── 新纪录提示
├── 本局数据
├── 能力评价
├── 成绩分享卡片
├── 分享成绩
├── 保存图片
├── 复制文案
├── 挑战好友
├── 再来一局
└── 返回首页
```

---

# 11. 首页设计

## 11.1 页面内容

首页展示：

* 游戏 Logo。
* 产品口号。
* 开始游戏按钮。
* 模式选择入口。
* 本地最高分。
* 今日挑战成绩。
* 最近一局成绩。
* 历史游戏次数。
* 设置按钮。
* 成就入口。
* 游戏规则入口。

## 11.2 开始游戏流程

玩家点击“开始游戏”后：

1. 显示 3、2、1 倒计时。
2. 自动聚焦答案输入框。
3. 播放开始音效。
4. 显示第一道题。
5. 正式开始倒计时。

## 11.3 首页背景

推荐使用轻量动画：

* 缓慢漂浮的数字和运算符。
* 随机出现的算式。
* 鼠标移动产生微弱视差。
* 背景根据最高分解锁不同主题。

动画应支持在设置中关闭。

---

# 12. 游戏页面设计

## 12.1 顶部信息区

显示：

* 剩余时间。
* 时间流逝倍率。
* 当前得分。
* 当前连击数。
* 答对题数。

示例：

```text
时间：18.4s    流速：×1.8
分数：3250     Combo：24
```

## 12.2 时间进度条

进度条根据剩余时间变化颜色：

| 剩余时间    | 颜色状态 |
| ------- | ---- |
| 大于 15 秒 | 绿色   |
| 8～15 秒  | 黄色   |
| 3～8 秒   | 橙色   |
| 小于 3 秒  | 红色闪烁 |

答对增加时间时，进度条应向右回弹，并显示：

```text
+2.0s
```

## 12.3 题目区域

题目位于页面视觉中心。

设计要求：

* 使用大字号等宽字体。
* 运算符与数字对齐。
* 题目切换时采用快速动画。
* 不应因数字位数变化导致整体页面明显跳动。

示例：

```text
58 × 17
```

## 12.4 答案输入框

要求：

* 游戏开始后自动获得焦点。
* 允许输入数字和负号。
* 按 Enter 提交答案。
* 移动端自动唤起数字键盘。
* 正确后立即清空。
* 错误后显示短暂红色反馈。
* 不允许输入字母和无关符号。

## 12.5 答题反馈

正确时：

* 显示“正确”“完美”“太快了”等随机提示。
* 题目短暂变绿。
* 时间奖励从题目位置飞向时间条。
* 连击数字放大。
* 播放正确音效。

错误时：

* 输入框左右震动。
* 页面边缘短暂变红。
* 显示正确答案。
* 连击清零。
* 播放错误音效。

---

# 13. 游戏结算页面

## 13.1 结算数据

游戏结束后展示：

* 最终得分。
* 正确题数。
* 错误题数。
* 正确率。
* 最长连击。
* 平均答题时间。
* 最快单题速度。
* 游戏持续时间。
* 最高时间流逝倍率。
* 是否刷新最高纪录。
* 获得的称号。
* 新解锁成就。

## 13.2 能力评价

系统根据成绩生成评价。

示例称号：

| 条件         | 称号    |
| ---------- | ----- |
| 得分较低       | 心算新手  |
| 答对 20 题    | 算术学徒  |
| 答对 40 题    | 心算高手  |
| 答对 70 题    | 人形计算器 |
| 连击达到 50    | 连击狂人  |
| 平均速度低于 1 秒 | 闪电大脑  |
| 答对 100 题   | 数学之神  |

评价示例：

```text
你的计算速度超过了自己历史记录中的 86%。
```

由于纯前端无法获得真实全站玩家数据，不应展示伪造的全球击败比例。可以使用本地历史成绩计算：

```text
本局成绩超过了你过去 86% 的对局。
```

---

# 14. 分享结果功能

## 14.1 功能目标

分享结果功能用于：

* 展示玩家的游戏成绩。
* 鼓励好友进行挑战。
* 提升产品传播能力。
* 让玩家保存个人最佳纪录。
* 将游戏结果转化为适合社交平台传播的视觉内容。

## 14.2 分享入口

以下位置提供分享入口：

1. 游戏结算页面。
2. 历史记录详情页。
3. 最高纪录页面。
4. 每日挑战完成页面。
5. 成绩卡片预览弹窗。

主要按钮：

* 分享成绩。
* 保存成绩图。
* 复制分享文案。
* 挑战好友。

## 14.3 成绩分享卡片

游戏结束后，前端自动生成一张成绩图片。

建议图片尺寸：

| 使用场景   |          尺寸 |
| ------ | ----------: |
| 通用竖版分享 | 1080 × 1440 |
| 手机海报   | 1080 × 1920 |
| 横版社交分享 |  1200 × 630 |
| 方形分享   | 1080 × 1080 |

默认使用 1080 × 1440 的竖版成绩卡。

## 14.4 分享卡片内容

成绩卡片包含：

* 游戏名称和 Logo。
* 本局模式。
* 最终得分。
* 答对题数。
* 正确率。
* 最长连击。
* 平均答题速度。
* 游戏持续时间。
* 玩家称号。
* 是否刷新纪录。
* 游戏日期。
* 挑战口号。
* 游戏访问地址或二维码。
* 卡片主题背景。

示例：

```text
Math Rush 算术冲刺

最终得分：5,280
答对题数：68
正确率：94%
最长连击：37
平均速度：1.12 秒

获得称号：闪电大脑

我坚持了 2 分 46 秒，
你能超过我吗？
```

## 14.5 卡片主题

根据玩家表现自动匹配卡片主题：

| 表现         | 分享卡主题    |
| ---------- | -------- |
| 普通成绩       | 深色极简     |
| 刷新纪录       | 金色荣耀     |
| 连击超过 30    | 火焰主题     |
| 平均速度低于 1 秒 | 闪电主题     |
| 每日挑战       | 每日挑战专属主题 |
| 答对超过 100 题 | 数学之神主题   |
| 极限模式高分     | 红黑极限主题   |

玩家可以在分享前切换已经解锁的主题。

## 14.6 分享卡片预览

点击“分享成绩”后打开预览弹窗。

弹窗内容：

* 成绩卡片预览。
* 切换卡片比例。
* 切换分享主题。
* 显示或隐藏部分数据。
* 修改分享文案。
* 保存图片。
* 系统分享。
* 复制文案。
* 复制挑战链接。

## 14.7 隐私控制

玩家可以控制分享卡片中是否显示：

* 游戏日期。
* 错误题数。
* 正确率。
* 历史纪录。
* 自定义昵称。
* 游戏地址二维码。

默认不要求玩家填写真实姓名。

昵称仅保存在本地。

## 14.8 保存成绩图片

使用 Canvas 或 DOM 截图方案在浏览器中生成图片。

建议实现方式：

* Canvas API。
* html-to-image。
* html2canvas。
* SVG 转 Canvas。

生成流程：

```text
读取本局游戏数据
  ↓
渲染分享卡片模板
  ↓
绘制背景、文字和图形
  ↓
生成 Blob
  ↓
下载 PNG 图片
```

默认文件名：

```text
math-rush-score-2026-07-23.png
```

## 14.9 系统分享

优先使用 Web Share API。

支持的浏览器中，点击“分享成绩”后调用系统原生分享面板。

分享内容包括：

* 分享标题。
* 分享文案。
* 游戏地址。
* 成绩图片文件。

示例逻辑：

```javascript
await navigator.share({
  title: "算术冲刺挑战",
  text: "我在算术冲刺中答对了 68 道题，你能超过我吗？",
  url: window.location.origin,
  files: [scoreImageFile]
});
```

兼容处理：

* 如果浏览器支持文件分享，则分享图片和文字。
* 如果只支持链接分享，则分享文字和网址。
* 如果不支持 Web Share API，则显示保存图片和复制文案按钮。

## 14.10 复制分享文案

系统根据成绩生成分享文案。

默认文案：

```text
我在「算术冲刺」中答对了 68 道题，
最高连击 37，平均每题只用了 1.12 秒！

你能超过我的 5280 分吗？
```

根据成绩生成差异化文案。

刷新纪录：

```text
新纪录！我在「算术冲刺」拿到了 5280 分，
答对 68 题，最长连击 37。
来挑战一下，看你能不能超过我！
```

高连击：

```text
连续答对 50 道题是什么体验？
我在「算术冲刺」中做到了。
现在轮到你了！
```

极限成绩：

```text
时间流速已经达到 3.5 倍，
我仍然坚持答对了 105 道题。
你敢来挑战吗？
```

每日挑战：

```text
我完成了今天的「算术冲刺」每日挑战，
得分 4260，答对 57 题。
今天的题目大家都一样，来比一比！
```

## 14.11 挑战链接

分享结果时可以生成挑战链接。

示例：

```text
https://example.com/?mode=classic&challenge=5280
```

挑战链接可携带：

* 游戏模式。
* 发起者得分。
* 发起者答对题数。
* 随机题目种子。
* 每日挑战日期。
* 分享卡主题编号。

为了避免 URL 过长，只传递必要参数。

进入挑战链接后，首页显示：

```text
好友向你发起挑战

目标分数：5280
答对题数：68

开始挑战
```

由于没有服务器，挑战数据来自 URL 参数，不应将其视为可信排行榜数据。

## 14.12 同题挑战

玩家可以选择“用同一套题挑战好友”。

实现方式：

1. 本局开始时生成随机种子。
2. 题目生成器根据种子生成固定题目序列。
3. 分享链接携带随机种子。
4. 好友打开链接后获得相同题目顺序。
5. 好友完成后比较双方成绩。

示例链接参数：

```text
?mode=seed&seed=MR20260723A8D4&target=5280
```

注意事项：

* 随机数生成必须使用可复现的伪随机算法。
* 游戏版本变化后，应在链接中携带题库版本。
* 示例参数：

```text
?mode=seed&seed=A8D4&target=5280&v=1
```

## 14.13 分享二维码

分享卡片可以生成游戏链接二维码。

实现建议：

* 使用纯前端二维码库。
* 二维码指向当前网站首页或挑战链接。
* 默认显示在卡片右下角。
* 玩家可以在分享设置中关闭二维码。

## 14.14 分享失败处理

可能出现的情况：

* 浏览器不支持 Web Share API。
* 浏览器不允许分享文件。
* 用户取消分享。
* 图片生成失败。
* 浏览器阻止下载。
* 移动端内置浏览器能力受限。

处理方式：

* 自动降级为复制文案。
* 提供“保存图片”按钮。
* 提供“复制挑战链接”按钮。
* 显示明确但不过度打扰的提示。
* 用户主动取消分享不显示错误警告。

## 14.15 分享埋点

纯前端版本不向服务器上传行为数据。

本地可以记录：

* 分享按钮点击次数。
* 保存图片次数。
* 复制文案次数。
* 复制挑战链接次数。
* 最常使用的分享卡主题。

这些数据仅用于本地成就和个人统计。

## 14.16 分享相关成就

| 成就    | 条件           |
| ----- | ------------ |
| 第一次炫耀 | 首次生成成绩卡      |
| 挑战发起人 | 首次复制挑战链接     |
| 战绩收藏家 | 保存 10 张成绩图   |
| 每日打卡  | 连续 7 天分享每日挑战 |
| 数学名片  | 解锁 5 种成绩卡主题  |

---

# 15. 历史记录

## 15.1 保存内容

保存最近 50～100 局游戏记录。

每条记录包含：

* 游戏模式。
* 最终分数。
* 答对数量。
* 错误数量。
* 正确率。
* 最长连击。
* 平均答题时间。
* 游戏持续时间。
* 最高时间倍率。
* 随机种子。
* 游戏日期。
* 是否刷新纪录。
* 分享卡片主题。

## 15.2 历史记录操作

玩家可以：

* 查看详情。
* 按模式筛选。
* 按分数排序。
* 重新生成分享卡片。
* 分享历史成绩。
* 删除单条记录。
* 清空全部记录。
* 使用该局题目种子再次挑战。

---

# 16. 本地排行榜

## 16.1 排行榜类型

纯前端版本支持：

* 历史最高分榜。
* 最长连击榜。
* 最快平均速度榜。
* 最长生存时间榜。
* 每日挑战本地纪录。
* 不同模式的独立排行榜。

## 16.2 排行榜范围

排行榜只展示当前设备和浏览器中的本地记录。

页面应明确标注：

```text
本机排行榜
```

不可宣传为全球排行榜。

---

# 17. 成就系统

## 17.1 基础成就

| 成就名称  | 解锁条件                |
| ----- | ------------------- |
| 第一次计算 | 完成第一局游戏             |
| 初次连击  | 连续答对 5 题            |
| 渐入佳境  | 连续答对 10 题           |
| 心算高手  | 单局答对 30 题           |
| 人形计算器 | 单局答对 60 题           |
| 数学之神  | 单局答对 100 题          |
| 闪电大脑  | 平均答题速度低于 1 秒        |
| 零失误   | 单局正确率达到 100%        |
| 绝地反击  | 剩余时间低于 1 秒后恢复到 10 秒 |
| 时间猎人  | 达到 3 倍时间流速          |
| 百局玩家  | 累计完成 100 局          |
| 分享达人  | 累计分享或保存成绩 20 次      |

## 17.2 成就奖励

成就可以解锁：

* 分享卡片主题。
* 游戏背景。
* 正确反馈特效。
* 数字字体。
* 音效包。
* 称号。
* 首页装饰。

所有奖励仅为视觉和个性化内容，不影响核心公平性。

---

# 18. 设置功能

设置页面包含：

## 18.1 游戏设置

* 开启或关闭加法。
* 开启或关闭减法。
* 开启或关闭乘法。
* 开启或关闭除法。
* 是否允许负数。
* 是否启用答错扣时。
* 是否启用特殊题目。
* 初始时间档位。
* 默认游戏模式。

至少需要开启一种运算类型。

## 18.2 音画设置

* 音乐开关。
* 音效开关。
* 音量大小。
* 动画开关。
* 粒子效果开关。
* 屏幕震动开关。
* 减少动态效果。
* 深色或浅色主题。

## 18.3 分享设置

* 默认分享卡比例。
* 默认分享主题。
* 是否显示日期。
* 是否显示正确率。
* 是否显示二维码。
* 是否显示自定义昵称。
* 分享文案语言。
* 是否在结算后自动生成分享卡。

---

# 19. 视觉设计

## 19.1 推荐风格

整体采用：

```text
极简霓虹街机风
```

视觉关键词：

* 深色背景。
* 高对比数字。
* 发光运算符。
* 动态时间进度条。
* 粒子反馈。
* 快速响应。
* 轻微科技感。
* 不影响数字识别。

## 19.2 推荐配色

| 元素   | 建议颜色      |
| ---- | --------- |
| 页面背景 | `#0F172A` |
| 卡片背景 | `#111827` |
| 主文字  | `#F8FAFC` |
| 正确状态 | `#22C55E` |
| 错误状态 | `#EF4444` |
| 时间警告 | `#F59E0B` |
| 强调色  | `#38BDF8` |
| 连击状态 | `#FACC15` |

## 19.3 字体

推荐字体：

* JetBrains Mono。
* Space Mono。
* IBM Plex Mono。
* Nunito Sans。
* 系统默认无衬线字体。

算术题和答案输入框优先使用等宽字体。

---

# 20. 音效与音乐

## 20.1 音效类型

* 答对音效。
* 答错音效。
* 连击升级音效。
* 时间增加音效。
* 时间不足警报。
* Boss 题出现音效。
* Fever 模式音效。
* 新纪录音效。
* 成就解锁音效。
* 分享卡生成音效。

## 20.2 音乐节奏

背景音乐可根据游戏状态动态变化：

* 游戏初期：平稳。
* 时间倍率提高：节奏加快。
* 剩余时间不足：加入紧张鼓点。
* Fever 模式：进入高潮。
* 游戏结束：音乐快速收束。

应确保关闭页面或切换后台时暂停音乐。

---

# 21. 数据存储设计

## 21.1 存储方案

小型配置和纪录使用 LocalStorage。

较多历史记录、成就与分享模板数据可使用 IndexedDB。

推荐划分：

| 数据类型    | 存储方式         |
| ------- | ------------ |
| 游戏设置    | LocalStorage |
| 最高纪录    | LocalStorage |
| 用户昵称    | LocalStorage |
| 最近游戏记录  | IndexedDB    |
| 成就状态    | IndexedDB    |
| 分享偏好    | LocalStorage |
| 自定义卡片资源 | IndexedDB    |

## 21.2 游戏记录数据结构

```typescript
interface GameRecord {
  id: string;
  mode: "classic" | "sprint100" | "oneLife" | "memory" | "daily";
  score: number;
  correctCount: number;
  wrongCount: number;
  accuracy: number;
  currentCombo: number;
  maxCombo: number;
  averageAnswerTime: number;
  fastestAnswerTime: number;
  duration: number;
  maxTimeScale: number;
  seed: string;
  questionVersion: number;
  title: string;
  isNewRecord: boolean;
  createdAt: string;
}
```

## 21.3 设置数据结构

```typescript
interface GameSettings {
  enabledOperators: Array<"add" | "subtract" | "multiply" | "divide">;
  allowNegative: boolean;
  wrongAnswerPenalty: boolean;
  specialQuestionsEnabled: boolean;
  soundEnabled: boolean;
  musicEnabled: boolean;
  animationEnabled: boolean;
  reducedMotion: boolean;
  initialTime: number;
  defaultMode: string;
  theme: string;
}
```

## 21.4 分享配置数据结构

```typescript
interface ShareSettings {
  nickname?: string;
  cardRatio: "portrait" | "story" | "square" | "landscape";
  cardTheme: string;
  showDate: boolean;
  showAccuracy: boolean;
  showQrCode: boolean;
  showNickname: boolean;
  language: "zh-CN" | "en";
}
```

---

# 22. 纯前端技术方案

## 22.1 推荐技术栈

可选方案一：

* React。
* TypeScript。
* Vite。
* Zustand。
* Tailwind CSS。
* Canvas API。
* IndexedDB。
* Web Audio API。
* Web Share API。
* PWA。

可选方案二：

* Vue 3。
* TypeScript。
* Vite。
* Pinia。
* UnoCSS。
* Canvas API。
* IndexedDB。
* Web Share API。

如果项目规模较小，也可以直接使用：

* HTML。
* CSS。
* JavaScript。

## 22.2 核心模块

```text
src/
├── components/
│   ├── QuestionDisplay
│   ├── AnswerInput
│   ├── TimeBar
│   ├── ScorePanel
│   ├── ComboDisplay
│   ├── ResultPanel
│   └── ShareCard
├── game/
│   ├── gameEngine
│   ├── questionGenerator
│   ├── difficultyManager
│   ├── timerManager
│   ├── scoreCalculator
│   ├── seededRandom
│   └── specialEventManager
├── share/
│   ├── cardRenderer
│   ├── shareTextGenerator
│   ├── webShare
│   ├── qrCodeGenerator
│   └── challengeLink
├── storage/
│   ├── settingsStorage
│   ├── recordStorage
│   └── achievementStorage
├── pages/
│   ├── Home
│   ├── Game
│   ├── Result
│   ├── History
│   ├── Achievements
│   └── Settings
└── styles/
```

## 22.3 计时器实现

不建议单纯使用：

```javascript
setInterval(() => time -= 0.1, 100);
```

因为浏览器可能出现计时漂移。

建议使用：

* `performance.now()` 计算真实时间差。
* `requestAnimationFrame()` 更新界面。
* 根据时间倍率计算本帧扣除时间。

伪代码：

```javascript
const delta = currentTimestamp - previousTimestamp;
remainingTime -= (delta / 1000) * timeScale;
```

页面切换到后台时，可以选择：

* 自动暂停游戏。
* 或根据实际经过时间继续扣时。

默认建议自动暂停，避免移动端误切后台导致游戏直接结束。

## 22.4 随机题目复现

为了支持同题挑战，需要实现可复现随机数生成器，例如：

* Mulberry32。
* Xorshift。
* SFC32。

题目生成不得直接依赖 `Math.random()`，否则无法根据种子复现。

## 22.5 成绩卡片生成

推荐流程：

1. 构建隐藏的分享卡 DOM。
2. 注入本局游戏数据。
3. 等待字体加载完成。
4. 将 DOM 转换为 Canvas 或 SVG。
5. 输出 PNG Blob。
6. 根据能力调用系统分享或下载。

生成前需确保：

* 字体已经加载。
* 背景图片没有跨域问题。
* 所有图标使用本地资源或内联 SVG。
* 生成过程不会将用户数据上传到第三方服务。

---

# 23. 响应式适配

## 23.1 PC 端

* 支持键盘数字输入。
* Enter 提交。
* Backspace 删除。
* Escape 暂停。
* 题目区域保持居中。
* 分享卡片在侧边或弹窗预览。

## 23.2 移动端

* 使用 `inputmode="numeric"`。
* 自动唤起数字键盘。
* 按钮触控区域不小于 44 × 44 像素。
* 避免键盘弹出后遮挡题目。
* 支持系统原生分享。
* 支持长按保存分享图片。
* 分享卡片优先采用竖版比例。

## 23.3 平板端

* 保持题目和输入区域居中。
* 横屏状态下使用左右分栏布局。
* 结算页面左侧显示数据，右侧显示分享卡片。

---

# 24. PWA 与离线能力

支持将网站安装到桌面或手机。

PWA 功能包括：

* 离线启动。
* 缓存页面资源。
* 全屏运行。
* 桌面图标。
* 启动画面。
* 本地保存成绩。

首次成功加载后，即使没有网络，也可以进行游戏和生成分享图片。

离线状态下：

* 可以保存成绩图片。
* 可以复制分享文案。
* 无法直接分享在线挑战链接时，应提示网络恢复后再分享。

---

# 25. 异常处理

## 25.1 本地存储不可用

处理方式：

* 游戏仍可正常进行。
* 提示“当前浏览器无法保存历史纪录”。
* 不阻止玩家开始游戏。

## 25.2 存储空间不足

处理方式：

* 优先删除最旧的历史记录。
* 不删除最高纪录和设置。
* 提示用户清理历史数据。

## 25.3 页面刷新

游戏过程中刷新页面：

* 当前游戏直接结束。
* 默认不恢复未完成对局。
* 可以记录异常中断次数，但不计入正式成绩。

## 25.4 浏览器切换后台

默认行为：

* 自动暂停。
* 回到页面后显示继续按钮。
* 每局最多允许自动暂停一定次数，避免利用后台暂停作弊。

## 25.5 输入法问题

* 只接受半角数字和负号。
* 自动转换全角数字。
* 忽略空格。
* 禁止科学计数法等异常输入。

---

# 26. 非功能需求

## 26.1 性能要求

* 首屏加载时间尽量低于 2 秒。
* 输入反馈延迟低于 50 毫秒。
* 题目切换过程中不出现明显卡顿。
* 动画目标帧率为 60 FPS。
* 分享卡片生成时间尽量低于 2 秒。
* 移动端中低性能设备可流畅运行。

## 26.2 可用性要求

* 玩家无需阅读完整教程即可开始。
* 首页到开始游戏不超过两次点击。
* 游戏过程中不出现弹窗广告或无关操作。
* 所有关键操作均支持键盘。
* 游戏结束后能够一键再来一局。

## 26.3 无障碍要求

* 文字与背景保持足够对比度。
* 不完全依赖颜色表达正确或错误。
* 支持减少动画模式。
* 所有按钮提供可读标签。
* 支持键盘焦点状态。
* 紧急倒计时闪烁频率不能过高。

## 26.4 隐私要求

* 不要求注册。
* 不收集手机号、邮箱等个人信息。
* 所有游戏数据默认仅保存在本地。
* 分享图片在本地浏览器生成。
* 不将成绩上传到服务器。
* 清空浏览器数据后，本地纪录可能丢失。

---

# 27. MVP 范围

## 27.1 MVP 必须实现

* 首页。
* 经典生存模式。
* 加减乘除随机题目。
* 答案输入与校验。
* 答对增加时间。
* 动态时间流逝倍率。
* 动态题目难度。
* 连击系统。
* 游戏结算页面。
* 本地最高分。
* 最近游戏记录。
* 基础音效和动画。
* 成绩分享卡片。
* 保存成绩图片。
* 复制分享文案。
* Web Share API 分享。
* 复制挑战链接。
* PC 和移动端适配。

## 27.2 MVP 暂不实现

* 全球在线排行榜。
* 用户登录。
* 云端同步。
* 多人实时对战。
* 好友系统。
* 后台管理系统。
* 在线题库管理。
* 服务端反作弊。
* 复杂社交关系。

---

# 28. 版本规划

## V1.0：核心玩法
* 经典生存模式。
* 四则运算题库。
* 动态时间倍率。
* 连击系统。
* 本地最高纪录。
* 结算统计。
* 成绩卡片。
* 保存和系统分享。
* 挑战链接。
* 幸运题。
* 炸弹题。
* 冰冻题。
* Boss 题。
* Fever 模式。
* 更多音效和动画。
* 更多成绩卡主题。
* 百题竞速。
* 极限一命。
* 闪现记忆。
* 每日挑战。
* 同题种子挑战。
* 成就系统。
* 主题解锁。
* 历史数据图表。
* 连续游玩天数。
* PWA 离线安装。
* 多语言支持。

---

# 29. 验收标准

## 29.1 核心玩法

* 玩家点击开始后能够正常进入游戏。
* 倒计时按照真实时间持续减少。
* 答对后正确增加剩余时间。
* 答错后按照规则进行处理。
* 答对题数增加后，时间流逝速度明显提高。
* 时间归零后游戏立即结束。
* 游戏过程中不会生成无法整除的除法题。
* 默认减法题不会产生负数结果。

## 29.2 输入体验

* PC 端可以仅使用键盘完成整局游戏。
* 移动端可以正常唤起数字键盘。
* 输入正确答案后，下一题立即出现。
* 输入错误不会造成页面卡死。
* 高频连续输入不会重复提交同一道题。

## 29.3 本地存储

* 刷新页面后最高分仍然存在。
* 历史记录能够正常保存和读取。
* 设置修改后可以持久化。
* 清空记录不会影响基础游戏功能。

## 29.4 分享功能

* 游戏结束后能够生成正确的分享卡片。
* 卡片数据与本局成绩一致。
* 分享卡片可以保存为 PNG。
* 支持 Web Share API 的设备能够调起系统分享面板。
* 不支持系统分享时能够复制文案和挑战链接。
* 分享链接能够打开指定模式。
* 同题挑战链接能够复现相同题目序列。
* 用户取消分享不会显示错误提示。
* 分享卡片生成过程不会请求服务器。

## 29.5 性能

* 连续游戏 10 分钟无明显内存异常。
* 快速输入时答题反馈无明显延迟。
* 分享图片生成时页面不会长时间无响应。
* 移动端竖屏和横屏均能正常使用。

---

# 30. 核心差异化

本产品与普通算术练习网站的区别在于：

1. **时间并非固定消耗。**
   玩家答得越多，时间流逝越快，后期压力会不断增加。

2. **答题本身就是生存资源。**
   玩家通过正确答案延长生命，而不是单纯在固定时间内刷题。

3. **难度根据能力动态变化。**
   系统综合答题速度、正确率和连击调整题目。

4. **具备街机游戏反馈。**
   连击、Fever、Boss 题和特殊事件让算术过程更有游戏感。

5. **分享结果是核心功能。**
   每局结束都能生成个性化成绩卡，并向好友发起同题挑战。

6. **完全纯前端。**
   无需服务器、登录和数据库，部署成本低，支持离线运行。

---

# 31. 推荐最终产品形态

建议首版采用以下组合：

```text
经典生存模式
+ 动态时间加速
+ 动态题目难度
+ 连击系统
+ 随机特殊题
+ Boss 题
+ 本地最高纪录
+ 个性化成绩卡
+ 保存图片
+ 好友挑战链接
+ PWA 离线安装
```

核心传播文案：

> 我在时间 3 倍加速的情况下答对了 68 道题。
> 这是我的成绩卡，你能超过我吗？

这版可以直接作为产品设计和前端开发依据。
